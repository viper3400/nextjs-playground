import { LoginCredentials } from '@/lib/types'

export const baseUrl = 'https://youserver.com/appi'

export const credentials : LoginCredentials = {
  username: 'Hans Wurst',
  password: 'secret',
  group: 'Default'
}